<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>PundiPundi — Dashboard</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#f4f3ee; --ink:#121313; --muted:#6d6f72;
      --lux:#d4af37; --stroke:rgba(212,175,55,.18); --hair:rgba(0,0,0,.08);
      --maxw:1180px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{ margin:0; background:var(--bg); color:var(--ink);
      font-family:"Inter",system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif; -webkit-font-smoothing:antialiased; line-height:1.35; }
    .wrap{ max-width:var(--maxw); margin:0 auto; padding:28px 24px 56px }

    /* NAV */
    .nav{
      display:grid; grid-template-columns:1fr auto 1fr; align-items:center;
      font-size:12px; letter-spacing:.06em; text-transform:lowercase;
      margin-bottom:clamp(32px,5vw,64px);
    }
    .nav__brand{font-weight:800; letter-spacing:.08em}
    .nav__right{justify-self:end}
    .nav__right a{ color:var(--ink); text-decoration:none; border-bottom:1px solid transparent }
    .nav__right a:hover{ border-bottom-color:var(--ink) }

    /* nav center buttons */
    .nav__actions{ display:flex; gap:10px; align-items:center }
    .pill{
      text-decoration:none; font-weight:600;
      padding:8px 12px; border-radius:999px;
      border:1px solid var(--stroke); color:var(--ink);
      background:transparent; transition:transform .06s ease,border-color .2s ease, box-shadow .2s ease;
    }
    .pill:hover{ transform:translateY(-1px); border-color:var(--lux) }
    .pill--gold{
      background:linear-gradient(90deg,var(--lux),#b9962f); color:#111; border-color:transparent;
      box-shadow:0 8px 24px rgba(212,175,55,.22);
    }
    .pill--gold:hover{ transform:translateY(-1px); box-shadow:0 10px 30px rgba(212,175,55,.28) }

    /* HERO */
    .hero{margin-top:clamp(6px,3vw,16px); margin-bottom:clamp(18px,4vw,28px)}
    .hero__word{ margin:0; font-weight:800; text-transform:lowercase; letter-spacing:-.02em;
      font-size:clamp(48px,14vw,140px); line-height:.92; }
    .hero__rule{ height:1px; background:#0f1010; opacity:.7; margin:10px 0 22px }
    .hero__meta{ margin:0; font-size:13px; color:var(--muted) }

    /* CARDS */
    .cards{ display:grid; grid-template-columns: repeat(2, minmax(0,1fr));
      gap: clamp(14px, 2.8vw, 24px); }
    @media (max-width: 760px){ .cards{ grid-template-columns: 1fr; } }

    .card{
      display:block; text-decoration:none; color:inherit;
      border:1px solid var(--stroke); border-radius:16px;
      padding: clamp(16px,3vw,22px);
      background:linear-gradient(180deg,rgba(255,255,255,.35),rgba(255,255,255,.12));
      box-shadow:0 12px 34px rgba(0,0,0,.05);
      transition:transform .08s ease, box-shadow .2s ease, border-color .2s ease;
      position:relative; overflow:hidden; min-height:134px;
    }
    .card::before{ content:""; position:absolute; inset:0;
      background:radial-gradient(400px 160px at -10% -20%, rgba(212,175,55,.08), transparent 60%); pointer-events:none; }
    .card:hover{ transform: translateY(-2px); border-color: var(--lux); box-shadow:0 16px 44px rgba(212,175,55,.20); }
    .card h3{ margin:0 0 6px 0; font-size:18px; font-weight:700; letter-spacing:.2px }
    .card p{ margin:0; font-size:13px; color:var(--muted) }
    .card .arrow{ position:absolute; right:14px; bottom:12px; font-size:18px; opacity:.6; transition: transform .12s ease, opacity .2s ease }
    .card:hover .arrow{ transform: translateX(2px); opacity:.9 }
  </style>
</head>
<body>
  <div class="wrap">
    <!-- NAV -->
    <nav class="nav">
      <div class="nav__brand">pundipundi</div>

      <!-- NAV CENTER: dua tombol -->
      <div class="nav__actions">
        <a class="pill" href="/userHome/<%= id %>">Home</a>
        <a class="pill pill--gold" href="/addCampaign/<%= id %>">create campaign</a>
      </div>

      <div class="nav__right">
        <a href="/logout">logout</a>
      </div>
    </nav>

    <!-- HERO -->
    <header class="hero">
      <h1 class="hero__word">Welcome!</h1>
      <div class="hero__rule"></div>
      <p class="hero__meta">Pilih tujuanmu hari ini.</p>
    </header>

    <!-- CARDS (4 item) -->
    <section class="cards">
      <a class="card" href="/myProfile/<%= id %>">
        <h3>My Profile</h3>
        <p>Lihat & lengkapi data pribadimu.</p>
        <div class="arrow">→</div>
      </a>

      <a class="card" href="/campaignList/<%= id %>">
        <h3>Campaign List</h3>
        <p>Jelajahi semua campaign yang aktif.</p>
        <div class="arrow">→</div>
      </a>

      <a class="card" href="/myDonations/<%= id %>">
        <h3>My Donations</h3>
        <p>Riwayat donasi dan detail kontribusimu.</p>
        <div class="arrow">→</div>
      </a>

      <a class="card" href="/myCampaign/<%= id %>">
        <h3>My Campaigns</h3>
        <p>Kelola campaign yang kamu buat.</p>
        <div class="arrow">→</div>
      </a>
    </section>
  </div>
</body>
</html>
